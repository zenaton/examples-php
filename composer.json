{
    "name": "zenaton/examples-php",
    "license": "Apache-2.0",
    "require": {
        "zenaton/zenaton-php": "^0.2.2"
    },
    "scripts": {
        "post-install-cmd": [
            "php -r \"file_exists('.env') || copy('.env.example', '.env');\""
        ]
    }
}
